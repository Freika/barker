<%= simple_form_for(@auction) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :realm_slug %>
    <%= f.input :realm_name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
